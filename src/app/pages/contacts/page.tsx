'use client';

import styled from 'styled-components';

const Wrapper = styled.section`
  width: 100%;
`;

const Contacts = () => {
  return (
    <Wrapper>
      <h1>Contacts</h1>
    </Wrapper>
  );
};

export default Contacts;
